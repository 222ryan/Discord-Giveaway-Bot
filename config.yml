# Discord bots command prefix
prefix: "!"

# The role which can run Giveaway commands
giveaway_role: "Giveaway"

# Set what the bot is currently playing
bot_status_text: "🎉 | Giveaways"

# IDLE, DND, ONLINE, OFFLINE
bot_activity: idle

# True/False - <prefix>help command
help_command: True

# How many seconds until the setup cancels due to no information being sent
setup_timeout: 15.0

# The emoji the user has to react with to enter the giveaway - Unicode
react_emoji: "🎉"

# True/False - Sends a message with a ping letting the winner know that they won.
ping_winner_message: True
